<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="de.micmun.android.miwotreff"
          android:versionCode="3"
          android:versionName="3.0">

    <uses-sdk
            android:minSdkVersion="14"
            android:targetSdkVersion="18"/>

    <permission
            android:name="de.micmun.android.miwotreff.permission.ACCESS_DB_PROVIDER"
            android:description="@string/perm_desc"
            android:protectionLevel="normal">
    </permission>

    <uses-permission android:name="de.micmun.android.miwotreff.permission.ACCESS_DB_PROVIDER"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
            android:allowBackup="true"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
        <activity
                android:name="de.micmun.android.miwotreff.MainActivity"
                android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- enable search dialog to send searches to SearchableActivity -->
            <meta-data
                    android:name="android.app.default_searchable"
                    android:value=".SearchActivity"/>
        </activity>
        <activity
                android:name="de.micmun.android.miwotreff.SearchActivity"
                android:exported="false"
                android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH"/>
            </intent-filter>
            <meta-data
                    android:name="android.app.searchable"
                    android:resource="@xml/searchable"/>
        </activity>
        <activity android:name="de.micmun.android.miwotreff.EditActivity"/>

        <provider
                android:name="de.micmun.android.miwotreff.utils.DataProvider"
                android:authorities="de.micmun.android.miwotreff.provider"
                android:permission="de.micmun.android.miwotreff.permission.ACCESS_DB_PROVIDER">
        </provider>
    </application>

</manifest>
